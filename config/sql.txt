alter table notices add short_description varchar(500);
create table notice_images (notice_id int primary key AUTO_INCREMENT, id int , image varchar(50));
alter table notice_images add constraint notice_id foreign key(id) references notices(id);
